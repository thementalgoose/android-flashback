package tmg.flashback.sandbox.core.presentation.styleguide

internal enum class StyleGuideTabs(
    val label: String
) {
    TEXT("Text"),
    INPUT("Input"),
    BUTTON("Button"),
    COLOURS("Colours");
}